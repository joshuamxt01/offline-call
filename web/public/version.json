{
  "versionCode": 9,
  "versionName": "0.2.7",
  "notes": "Fixes the crash when sending a voice note, video, or photo (they now upload and send correctly). Incoming calls now show the caller's name instead of a generic label. Smaller, faster Windows download. Tap Update to install."
}
