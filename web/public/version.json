{
  "versionCode": 8,
  "versionName": "0.2.6",
  "notes": "Fixes 'Unable to decrypt' messages. The app now always uses each person's current encryption key, so messages send and read correctly even after someone reinstalls. Update BOTH devices, then send a new message (older undecryptable messages can't be recovered)."
}
